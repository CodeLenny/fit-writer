/**
 * A field used in a Fit file.
 * {@code FitField}s can be used either when definining {@link FitRecordDefinition}s,
 * or when instantiating definitions to create {@link FitDataRecord}s.
 */
export default interface FitField {

    __field: "field";

}
