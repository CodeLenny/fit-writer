import FitBaseType from "../FitBaseType";

/**
 * An enum type.
 */
export default class FitEnum extends FitBaseType {

    protected static readonly typeName = "enum";
    protected static readonly typeValue = 0;

}
